"""MCP tool modules.

Each module exposes ``register(mcp, *clients)`` which attaches its task-oriented tools to
the shared ``FastMCP`` instance. Tools are organized by capability, not by data source.
"""
